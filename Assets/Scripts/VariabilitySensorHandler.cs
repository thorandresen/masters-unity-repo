using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class VariabilitySensorHandler : MonoBehaviour
{
    [SerializeField]
    List<GameObject> choices;

    [SerializeField]
    Material active;

    [SerializeField]
    Material normal;

    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
